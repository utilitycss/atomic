.fontFamily {
  electrons: (fontFamily_system);
}

.body {
  composes: fontFamily;
  electrons: (
    fontSize_s,
    black
  );
}

.title {
  composes: fontFamily;
  electrons: (
    fontSize_l,
    black
  );
}
